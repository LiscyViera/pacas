<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="stylesheet" href="resources/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous">
  <style>
    .color-btn {
      background-color: #00a09d;
    }

    .color-head {
      background-color: #875a7b;
    }
  </style>
</head>

<body class="body">
  <div class="d-flex justify-content-center mt-5 rounded-3 text-white" style="opacity: 0.9;">
    <div class="d-flex flex-column w-25 text-center bd-highlight mb-3">
      <div class="form-control">
        <div class="p-2 color-head text-white rounded-3 bd-highlight">
          <h5 style="font-weight: bold;">Inicio de Sesión</h5>
        </div>
        <form action="auth" method="POST">
          <label for="user" style="font-weight: bold; font-size: 13px;" class="mb-3">Nombre de usuario</label>
          <input type="text" name="user" placeholder="User" class="form-control text-center mb-3"
            style="font-weight: bold;font-size: 13px;" autofocus required>
          <label for="pass" style="font-weight: bold; font-size: 13px;" class="mb-3">Contraseña</label>
          <input type="password" name="password" class="form-control text-center mb-3" placeholder="Password"
            style="font-weight: bold;font-size: 13px;" required>
          <input type="submit" class="btn color-btn text-white form-control" style="font-weight: bold; font-size: 13px;"
            value="LOGIN">
        </form>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

   <% if (typeof alert != "undefined")  { %>
    <script>
    Swal.fire({
      title:'<%= alertTitle %>',
      text:'<%= alertMessage %>',
      icon:'<%= alertIcon %>',
      showConfirmButton: <%= showConfirmButton %>,
      timer: <%= timer %>
    }).then(()=>{
      window.location='/<%= ruta %>'
    })
    </script>
  <% } %>
      <%- include('../footer') %>
</body>

</html>