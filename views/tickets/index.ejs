<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FUNDAS DE CAPA</title>
    <link rel="stylesheet" href="resources/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css"
        integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">
   


    <style>
        .color-btn {
            background-color: #00a09d;
        }

        .color-head {
            background-color: #875a7b;
        }

        table {
            font-family: Arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        thead {
            background-color: #875a7b;
            color: white;
        }

        th,
        td {
            text-align: left;
            padding: 8px;
        }

        th {
            font-weight: bold;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        tr:hover {
            background-color: #ddd;
        }

        td:last-child {
            text-align: center;
        }

        td:last-child a {
            color: #875a7b;
            text-decoration: none;
        }

        td:last-child a:hover {
            text-decoration: underline;
        }
        .pagination {
            display: inline-block;
        }

        .pagination {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }
        .page-link {
            display: block;
            padding: 10px;
            margin: 0 5px;
            border: 1px solid #ccc;
            text-decoration: none;
            color: #333;
        }
        .page-link.active {
            background-color: #ccc;
            color: #fff;
        }
    </style>
</head>

<body class="body">
    <%- include('navbar')%>

        <div class="container">
            <br><h5 style="color: dimgrey;">Etiquetas gavillas fundas</h5> 

           
            <a style="background-color: #00a09d; color: white;" class="btn"href="/tickets/crear">Crear</a>
            <br>
            <br>
            <table id="table">
                <thead>
                    <tr>
                        <th>Número de paca</th>
                        <th class="ordenar-descendente">Fecha de elaboración</th>
                        <th>Gavillas Paca</th>
                        <th>Editar|Imprimir</th>
                    </tr>
                </thead>
                <tbody id="tableBody"></tbody>

                <% for( let ticket=0; ticket < ticket1.length; ticket++ ) { %>
                    <tr>
                        <td>
                            <%=ticket1[ticket].n_paca%>
                        </td>
                        <td>
                            <%=ticket1[ticket].fecha_elaboracion%>
                        </td>
                        <td>
                            <%=ticket1[ticket].gavillas_paca%>
                        </td>
                        <td> <div class="btn-group-vertical" role="group" aria-label="Vertical button group">
                            <form class="form-inline" method="get" action="/tickets/<%=ticket1[ticket].id%>" enctype="multipart/form-data">
                                <button class="btn btn-light" type="submit">Ver</button>
                            </form>
                        </div></td>
                    </tr>
                    <% } %>
                        </tbody>
            </table>
           
            <div class="pagination">
                <% if (pageInfo.hasPreviousPage) { %>
                  <a href="?page=<%= pageInfo.previousPage %>">Anterior</a>
                <% } else { %>
                  <span class="disabled">Anterior   </span>
                <% } %>
              
                <% for (let i = pageInfo.startPage; i <= pageInfo.endPage; i++) { %>
                  <% if (i === pageInfo.currentPage) { %>
                    <span class="current"><%= i %></span>
                  <% } else { %>
                    <a href="?page=<%= i %>"><%= i %></a>
                  <% } %>
                <% } %>
                <% if (pageInfo.hasNextPage) { %>
                  <a href="?page=<%= pageInfo.nextPage %>">Siguiente</a>
                <% } else { %>
                  <span class="disabled">Siguiente</span>
                <% } %>
              </div>
              
        </div>
</body>

</html>